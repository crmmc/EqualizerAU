# EqualizerAU 领域词汇

本文统一 EqualizerAU 产品与音频处理链中的领域语言，避免把处理效果与系统控制混为一谈。

## Language

**前置放大（Preamp / Preamplification）**：
处理链中以 dB 表示、宽带调整音频信号电平的基础滤镜；名称与 EqualizerAPO 编辑器
保持一致，不代表 macOS 系统音量或物理设备音量。
_避免：增益节点、Gain Node、音量节点、Volume 节点、前级增益_

**系统音量**：
由 macOS 或输出设备控制的播放音量，不属于 EqualizerAU 处理链。
_避免：前置放大、Preamp、节点音量_

**声道标识（Channel Identifier）**：
用于选择处理目标的声道名称。已知扬声器位置使用 `L`、`R`、`C`、`LFE` 等语义名称；
无法确定扬声器位置时使用从 `1` 开始的数字名称。用户也可创建非空的自定义标识，
保存时规范化为大写；它在当前布局无法解析时仍按未解析声道标识保留。`all` 表示当前
布局的全部声道。
_避免：在不知道扬声器位置时按声道数量猜测名称_

**未解析声道标识（Unresolved Channel Identifier）**：
配置中存在、但当前输出布局没有对应语义位置或数字位置的声道标识。它不会被重定向到
其他声道；同一选择中的其他可解析标识仍然有效，全部未解析时该节点为空操作。
_避免：无效声道、自动回退到全部声道_

**配置草稿（Configuration Draft）**：
用户正在编辑、尚未显式保存的处理链工作副本。草稿不改变当前声音，也不会在重启后恢复。
_避免：当前配置、实时配置_

**已保存配置（Saved Configuration）**：
最近一次成功保存的处理链，是重启恢复和运行时应用的期望状态；它可能因音频生命周期
转换而暂时处于“待应用”。
_避免：草稿、临时配置_

**恢复配置（Recovery Configuration）**：
应用无法建立正常持久配置时，在内存中提供的待修复配置。它可能来自最近一份仍可读取
的完整配置或透明安全默认值，但不是正常已保存配置；用户编辑仍不改变当前声音，只有
显式 Repair 成功后它才成为新的已保存配置。
_避免：已保存配置、普通未修改草稿、静默空链_

**节点启用状态（Node Enabled State）**：
决定一个已保存节点是否参与信号处理的节点属性。停用节点仍保留在处理链中，但不改变
信号；编辑该状态只改变配置草稿，显式保存后才应用。它不等同于删除节点或关闭音效总开关。
_避免：音效总开关、删除节点_

**音效总开关（Effects Switch）**：
立即在已保存处理链与透明处理之间切换的总控状态。它不提交配置草稿，也不启动或停止
底层系统音频链路；应用重启后恢复磁盘中最近一次完整保存的状态。
_避免：急停、系统处理启停、全局旁路_

**系统处理启停（System Processing Lifecycle）**：
创建或销毁 EqualizerAU 系统音频链路的生命周期操作，与音效总开关相互独立。
_避免：音效开关、旁路_
