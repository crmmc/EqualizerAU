# EqualizerAU 领域词汇

本文统一 EqualizerAU 产品与音频处理链中的领域语言，避免把处理效果与系统控制混为一谈。

## Language

**前置放大（Preamp / Preamplification）**：
处理链中以 dB 表示、宽带调整音频信号电平的基础滤镜；名称与 EqualizerAPO 编辑器
保持一致，不代表 macOS 系统音量或物理设备音量。
_避免：增益节点、Gain Node、音量节点、Volume 节点、前级增益_

**系统音量**：
由 macOS 或输出设备控制的播放音量，不属于 EqualizerAU 处理链。
_避免：前置放大、Preamp、节点音量_

**图形均衡器（Graphic EQ）**：
处理链中的固定中心频率效果节点。首版包含 15 个频段，每段以 dB 独立调整并继承当前位置的
Channels 作用域；全部频段为 `0 dB` 时不改变信号。当前采样率下等于或高于 Nyquist 的频段
保留在配置中但不参与处理，并显示为不可用。
_避免：参数均衡器、频谱分析器、把不可用频段重定向到较低频率_

**卷积（Convolution）**：
使用不可变 WAV 脉冲响应处理音频的效果节点。单声道 IR 广播到当前 Channels 作用域；多声道
IR 必须与该作用域当前可解析的声道数严格相等，并按作用域顺序映射。节点保存 IR 身份与来源
元数据，不保存外部文件路径或音频字节。
_避免：混响预设、任意音频文件、按声道数猜测或循环映射 IR_

**脉冲响应（Impulse Response / IR）**：
导入后保存在应用数据目录中的不可变 WAV sidecar。配置通过 storage ID、SHA-256 和来源元数据
引用它；替换 IR 会产生新的 sidecar 和编辑历史步骤，不原地覆盖已有资源。
_避免：配置附件、外部路径引用、可变共享文件_

**声道标识（Channel Identifier）**：
用于选择处理目标的声道名称。已知扬声器位置使用 `L`、`R`、`C`、`LFE` 等语义名称；
无法确定扬声器位置时使用从 `1` 开始的数字名称。用户也可创建非空的自定义标识，
保存时规范化为大写；它在当前布局无法解析时仍按未解析声道标识保留。`all` 表示当前
布局的全部声道。
_避免：在不知道扬声器位置时按声道数量猜测名称_

**未解析声道标识（Unresolved Channel Identifier）**：
配置中存在、但当前输出布局没有对应语义位置或数字位置的声道标识。它不会被重定向到
其他声道；同一选择中的其他可解析标识仍然有效，全部未解析时该作用域内效果为空操作。
_避免：无效声道、自动回退到全部声道_

**声道作用域节点（Channels Scope Node）**：
处理链中的非 DSP 控制节点。它选择后续效果的目标声道，直到下一个 Channels 节点覆盖该
作用域；效果节点只展示继承结果，不各自保存声道选择。复制效果但不复制 Channels 时，效果
继承目标位置的作用域；连同 Channels 一起复制时保留显式作用域。
_避免：声道效果、每效果声道副本、声道滤镜_

**配置草稿（Configuration Draft）**：
用户正在编辑、尚未显式保存的处理链工作副本。草稿不改变当前声音，也不会在重启后恢复。
_避免：当前配置、实时配置_

**已保存配置（Saved Configuration）**：
最近一次成功保存的处理链，是重启恢复和运行时应用的期望状态；它可能因音频生命周期
转换而暂时处于“待应用”。
_避免：草稿、临时配置_

**恢复配置（Recovery Configuration）**：
应用无法建立正常持久配置时，在内存中提供的待修复配置。它可能来自最近一份仍可读取
的完整配置或透明安全默认值，但不是正常已保存配置；用户编辑仍不改变当前声音，只有
显式 Repair 成功后它才成为新的已保存配置。
_避免：已保存配置、普通未修改草稿、静默空链_

**节点启用状态（Node Enabled State）**：
决定一个已保存节点是否参与信号处理的节点属性。停用节点仍保留在处理链中，但不改变
信号；编辑该状态只改变配置草稿，显式保存后才应用。它不等同于删除节点或关闭音效总开关。
_避免：音效总开关、删除节点_

**Processing 控件（Processing Control）**：
主界面的唯一处理总控。系统处理停止时，开启它会启动链路并应用已保存效果；链路运行时，
关闭它只立即旁路效果并保持链路，重新开启恢复已保存处理。旁路状态不提交节点草稿；应用
重启后仍以磁盘中最近一次完整保存的状态为准。
_避免：把旁路描述成 Stop、把底层 Start/Stop 暴露为第二个主开关_

**系统处理启停（System Processing Lifecycle）**：
创建或销毁 EqualizerAU 系统音频链路的高级生命周期/恢复操作。它与运行中的 Processing
旁路不同，不作为主窗口的第二个总开关。
_避免：Processing 旁路、普通 A/B 切换_

**自动音频恢复（Automatic Audio Recovery）**：
系统处理先前正在运行时，由默认输出、设备格式、Core Audio 服务或 wake 事件触发的有界
stop/re-discover/start。它只重新应用最近运行基线，不保存配置草稿；预算耗尽、权限拒绝、
显式 Stop 或 Quit 后不会继续自动重试。
_避免：自动保存、无限重启、在 sleep 期间启动_
